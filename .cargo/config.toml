# Applied only when that target is selected (cross / rustup target).
# Host builds on macOS/x86_64 are unaffected.
[target.aarch64-unknown-linux-gnu]
rustflags = ["-C", "target-cpu=cortex-a53"]

[target.aarch64-unknown-linux-musl]
rustflags = ["-C", "target-cpu=cortex-a53"]
